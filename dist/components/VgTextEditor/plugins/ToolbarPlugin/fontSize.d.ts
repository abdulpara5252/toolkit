import { LexicalEditor } from 'lexical';
declare function FontSize({ editor, value, style, disabled, }: {
    editor: LexicalEditor;
    value: string;
    style: string;
    disabled?: boolean;
}): JSX.Element;
export default FontSize;
